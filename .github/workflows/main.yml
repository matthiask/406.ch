name: Pages
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: pip
      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip wheel
          python3 -m pip install -r requirements.txt
      - name: Build site
        run: python generate.py
      - name: Deploy
        uses: peaceiris/actions-gh-pages@627df3d602afb02fc7cb2dce093cdf1716834e2e
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./htdocs/
          cname: 406.ch
